<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="myjdbc.properties"></properties>

    <typeAliases>
        <package name="com.itheima.domain"/>
    </typeAliases>

    <environments default="mysql1">
        <environment id="mysql1">
            <transactionManager type="JDBC"></transactionManager>
            <dataSource type="POOLED">
                <property name="drive" value="${myjdbc.driverClassName}"/>
                <property name="url" value="{myjdcb.url}"/>
                <property name="username" value="{myjdcb.username}"/>
                <property name="password" value="{myjdcb.password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <package name="com.itheima"/>
    </mappers>

</configuration>